# 4×4 天线阵列：I 代 IPEX 接口版本

日期：2026-09-20。工程版本：IPEX1-A2。KiCad 10.0.0。

打开 `Phased_Array_Ant_IPEX1.kicad_pro`。此目录包含完整原理图、PCB、项目局部符号库及封装库，库路径使用 `${KIPRJMOD}`，可整目录移动。

**图纸定位已修正：**原 Eagle 坐标的板框位于 X=0～70、Y=-70～0 mm，导致显示在图纸外。现已将全板统一平移 X=+113.5、Y=+135.0 mm，板框变为 X=113.5～183.5、Y=65～135 mm，位于 A4 横向图纸中央。辅助原点和栅格原点同步平移。594 个对象的回读验证确认相对几何、尺寸、网络和 UUID 不变；本说明中与原板“位置保持一致”均指扣除本次整体平移后的一致性。备份位于 `verification/before_page_centering/`，记录位于 `verification/page_centering.json`。

## 修改内容

- 经用户选择，以 `Gerber_Patch_Antenna_70x70_4L_Via03_20260918/source/Phased_Array_Ant_70x70_4L_Via03.kicad_pcb` 为基础，四层、70×70 mm，原有馈电过孔孔径 0.30 mm。
- J1～J16 从原 WR-SMP 座子替换为 ECT（电连）`818000368`，USS RF I 代，三焊脚，高度 1.25 mm；物料编码 `YC.DZ.SS00008`。按所提供产品规格书 SPEC E 建库，并非把通用 IPEX 型号当成该物料的完整型号。
- 连接器位号、相对中心坐标、底面安装位置，以及 N$1～N$16 与原 U$1～U$16 天线的对应关系保留。原来的阵列位置值（如 4_1）存入 `ArrayPosition` 字段。
- 原理图按物理阵列的 4×4 排布重新绘制为 KiCad 原生格式：上排 J13～J16，下排 J1～J4。两个接地焊脚均在原理图中显示。符号 UUID 已与 PCB 封装关联，支持后续从原理图更新 PCB。
- 16 条底层信号线延伸到新信号焊盘中心。删除旧连接器的 45 条底层接地线，每个新座子增加两条 0.30 mm 宽的接地线和两个就近贯穿接地过孔，共新增 32 个孔径/外径 0.30/0.60 mm 的 GND 过孔。
- 新地孔相对连接器中心位于 PCB 坐标 `(±2.50, -1.00) mm`，避开相邻行的顶层天线铜皮。原有 16 个信号过孔及 12 个 GND 过孔保持原状。
- 原始 Eagle 文件、已有 KiCad 导入目录及此前生产文件未覆盖。新工程为独立可编辑版本；此前光绘包不包含这次接口修改。

## 符号和 PCB 封装

- 符号：`PLFM_RF.kicad_sym`，器件名 `PLFM_RF:ECT_818000368`。
- 封装：`PLFM_RF.pretty/ECT_818000368_IPEX1_1.25mm.kicad_mod`。
- 配套库表：`sym-lib-table`、`fp-lib-table`。
- 原天线铜皮也收录为项目局部符号及封装 `Patch_10p5GHz_9.538x7.636mm`，便于继续编辑。
- 规格书原件的相同内容副本：`datasheets/ECT_818000368_SPEC_E.pdf`。

尺寸依据规格书第 7 页的推荐 PCB LAYOUT。以下为封装库顶面视图坐标，原点为连接器中心，信号焊盘朝 +Y；安装到底层时由 KiCad 翻转。

| 本库焊盘号 | 网络功能 | 中心 X / Y，mm | 焊盘宽 × 高，mm |
|---|---|---|---|
| 1 | SIG | 0 / 1.525 | 1.00 × 1.05 |
| 2 | GND | -1.475 / 0 | 1.05 × 2.20 |
| 3 | GND | +1.475 / 0 | 1.05 × 2.20 |

两接地焊盘的总外宽为 4.00 mm、内侧间距为 1.90 mm；信号焊盘近端距中心 1.00 mm。焊盘使用规格书所画的矩形形状，阻焊单边扩展 0.05 mm，锡膏与焊盘等大。

**1/2/3 是本库定义的电气编号。**规格书机械剖面里的圈号 ①/②/③ 对应绝缘体、接触件、外壳的材料表，不作为电气焊脚编号引用。

元件下方设置安装面铜禁布区：X ±0.94 mm、Y -1.10～+0.99 mm，禁止走线、过孔、焊盘及铺铜，允许放置封装。该区域以推荐焊盘的间隙保守定义，距两侧地焊盘及信号焊盘各留 0.01 mm 的数值边界；不是规格书单独标注的禁布区尺寸，也不扩展为内层地平面的禁铜。装配轮廓及 courtyard 用于工程显示，未加入未经核实的 3D 模型。

## 检查结果

KiCad ERC：**0**；PCB DRC：**0**；未连接项：**0**；原理图与 PCB 一致性问题：**0**。

独立回读校验还确认：

- 扣除整体平移后，16 个天线贴片的坐标、尺寸 9.538×7.636 mm、铜层和网络与基础版完全一致。
- 顶层信号线、原有信号过孔、板框、绘图对象及 4 个 3.20 mm 非金属化安装孔保持一致。
- 17 个网络在原理图导出网表与 PCB 上的全部节点完全匹配：每路信号连接 1 个座子信号端及 1 个天线；GND 连接 32 个座子外壳焊盘。
- 每个新座子的三个焊盘尺寸、底面层组和镜像方向逐项匹配。
- 最终共有 60 个金属化贯穿孔，孔径全部 0.30 mm；其中 16 个馈电孔、44 个 GND 孔。
- 源板、源原理图及所给 PDF 的 SHA-256 可追溯；原文件未改动。

报告位于 `verification/erc.json`、`drc.json`、`final_verification.json`；网表为 `schematic_netlist.xml`；修改位置及来源哈希记录在 `build_audit.json`。DRC 使用项目生效规则；A2 已启用原先默认/继承关闭的五类 PCB DRC 检查，未添加违规排除项。

`verification/build_ipex.py` 是重建脚本，会覆盖本输出工程，只应在备份并关闭编辑器后用于重建；用 KiCad 自带 Python 执行。`verify_ipex.py` 为只读验证脚本。重新生成后须执行 KiCad CLI 的铺铜、DRC、ERC 及网表导出，报告并非由重建脚本自动更新。

## 使用边界

**818000368 规格书的频率范围是 DC～6 GHz，原工程天线标为 10.5 GHz。**本次完成了封装替换、电气连通与几何规则检查，没有验证该连接器/线缆或新馈电过渡在 10.5 GHz 的回波损耗、插损及阵列性能。不能据此把新接口视为原接口在 10.5 GHz 的等效替代。

用户已指定打样参数：FR4、成品板厚 1.0 mm、外层铜厚 1 oz、内层铜厚 0.5 oz、OSP；层序 F.Cu → In1.Cu → In2.Cu → B.Cu。PCB 总板厚显示值已由原默认 1.6 mm 更新为 1.0 mm，仅修改厚度元数据。各介质层厚度、FR4 具体牌号及阻抗参数尚未指定；不要把 KiCad 自动生成的各介质层默认厚度当成已确认的射频叠层。生产光绘另存于 `4_7_Production Files/Gerber_Patch_Antenna_IPEX1_70x70_4L_Via03_20260920_R2`。用于该频段前，应核对连接器与线缆频率适配，并进行馈电过渡仿真/实测。

预览在 `preview/`：原理图、底层 PCB 和独立连接器封装的 SVG/PNG。

## A2 全面复核修正

保留四层，L2 / In1.Cu 与 L3 / In2.Cu 为整面 GND，44 个地孔逐个验证直接连接两层；16 个信号孔在两层均保持隔离。

修正打印天线铜皮继承的锡膏层：16 个天线焊盘及局部库已去掉 F.Paste，天线铜形和阻焊开窗不变。补齐天线与安装孔的 CAD courtyard，并建立局部安装孔封装；天线轮廓按铜边外扩 0.25 mm，安装孔按孔半径外扩 0.25 mm，仅用于 CAD 重叠检查，不代表射频净空或螺钉头空间。

工程已保存与生产流程一致的 9 层出图选择、辅助原点、X2、取消镜像/图框/光绘钻孔标记、丝印减阻焊开窗。已确认加工参数写入 工程文本变量及 PCB 标题栏。Gerber job 仍需经配套导出脚本清除未经确认的默认介质层厚度。

复核脚本为 verification/review_fixes.py；重建脚本已调用该修正步骤。复核资料在 verification/full_review_20260920，生产文件使用 R2 包。
