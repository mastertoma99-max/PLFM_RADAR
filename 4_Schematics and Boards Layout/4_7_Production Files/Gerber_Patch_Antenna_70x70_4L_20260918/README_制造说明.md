# 70×70 mm 阵列天线板：四层光绘资料

生成日期：2026-09-18。状态：光绘已生成并完成回读检查；按用户要求，板材及成品板厚待确认。

## 本次修改

- 对象：`Antennas/Patch/4x4/Phased_Array_Ant.brd`，70×70 mm、16 个贴片天线单元。
- 在副本中将 EAGLE 叠层从 `(1+2*3+14*15+16)` 改成 `(1+2*15+16)`，停用原先没有走线/覆铜的 3、14 层。
- 原始文件保持不变；`source/Phased_Array_Ant_original_6L.brd` 为原文件备份。
- 天线、器件坐标、走线、过孔、网络和板框未改动。两个 GND 平面保留。

## 光绘层序

所有光绘采用相同坐标系，不镜像；钻孔与光绘原点一致。成品板框按外形线中心线为 **70.000×70.000 mm**，不要把外形线宽计入板尺寸。

| 物理层/用途 | 原 EAGLE 层 | 文件后缀 |
|---|---:|---|
| L1 顶层铜，天线及馈线 | 1 | `-F_Cu.gtl` |
| L2 内层铜，GND | 2 | `-In1_Cu.g1` |
| L3 内层铜，GND | 15 | `-In2_Cu.g2` |
| L4 底层铜，连接器及馈线 | 16 | `-B_Cu.gbl` |
| 顶层/底层阻焊开窗 | — | `-F_Mask.gts` / `-B_Mask.gbs` |
| 顶层/底层丝印 | — | `-F_Silkscreen.gto` / `-B_Silkscreen.gbo` |
| 板外形 | 20 | `-Edge_Cuts.gm1` |
| 金属化通孔 | — | `-PTH.drl` |
| 非金属化安装孔 | — | `-NPTH.drl` |

文件名前缀均为 `Phased_Array_Ant_70x70_4L`。铜层、阻焊、丝印、外形为 Gerber X2 / RS-274X 兼容格式；钻孔为毫米制 Excellon。`-job.gbrjob` 提供四层及文件映射，已经移除软件自动填入但未经确认的板材、板厚、表面处理和设计规则字段。

## 钻孔

| 类型 | 文件孔径 | 数量 |
|---|---:|---:|
| 金属化通孔 | 0.20 mm | 16 |
| 金属化通孔 | 0.30 mm | 12 |
| 非金属化安装孔 | 3.20 mm | 4 |

合计 32 孔。安装孔中心为 `(2,2)`、`(2,68)`、`(68,2)`、`(68,68)` mm。所有孔保持原设计尺寸；16 个 0.20 mm 通孔低于原文件继承的 0.30 mm 最小钻孔规则，需板厂确认加工能力及孔径/公差要求。本次没有扩大孔径或移动孔位。

## 材料与叠层确认

板材型号、介电参数、成品厚度、各介质层厚度、铜厚、表面处理及加工公差尚未确定，**不得直接套用普通 FR-4 或任意默认四层叠层投产**。这些参数需要由用户与板厂确认；本资料包不宣称已经完成删层后的射频性能复核。

修改后的 EAGLE 文件仍保存原设计规则中的铜厚/介质厚度数值，仅用于追溯；这些继承数值不构成确认的四层加工叠层。KiCad 文件里的 1.6 mm 板厚是导入软件默认值，只用于编辑/预览，也不构成制造要求。光绘包的 job 文件已删除这些未经确认的默认制造参数。

## 导出与检查结果

导出使用本机 KiCad 10.0.0 的 EAGLE 导入器及 Gerber/Excellon 导出器；不是 EAGLE 原生 CAM 导出。KiCad 导入时把连接器封装中的接地铜形状保留为图形，本次为 16 个接地铜形状恢复 GND 网络属性，未改动形状。覆铜按原 EAGLE 的 0.10 mm 隔离、0.20 mm 多边形线宽、0.30 mm 铜到板边规则重算；阻焊扩展恢复为原设置 0.0508 mm。

已完成：

- 原始与四层 EAGLE 文件的 `plain/libraries/elements/signals` 内容逐项一致。
- 原始与四层导入板的器件、焊盘、走线和过孔几何及网络一致。
- 独立比对原 EAGLE 中全部 101 段走线、28 个过孔的坐标、宽度/孔径及网络。
- 同一导出流程下，删层前后保留的四个铜层及双面阻焊 Gerber 图元完全一致。
- 用独立 Gerber 解析器回读，按实际铜图形与金属化过孔构建连接图：80 个电气焊盘、28 个过孔所属的 17 个网络全部连通，未发现网络间短路。
- 检查双面阻焊开窗的实际图形，未发现单个连续开窗同时暴露两个不同网络。
- 独立 Excellon 回读确认 32 孔的孔径、孔数及坐标；外形闭合且中心线尺寸为 70×70 mm。
- 已查看实际 Gerber 渲染的四层预览。

**DRC 并非零报告：**删层前后的 KiCad DRC 统计相同，均有 16 条 0.20 mm 孔径小于继承规则的报告、60 条连接器阻焊图形报告，以及 24 条连接器地焊盘连接报告。后两类来自 EAGLE 封装铜/阻焊图形的导入表达，已用实际光绘连通图和阻焊图形检查复核；原始 DRC JSON 完整保留，未隐藏或删除。此项检查不代替板厂 DFM、EAGLE 原生 DRC 或射频实测。

## 文件位置

- `Phased_Array_Ant_70x70_4L_Gerber.zip`：发给板厂评审的光绘、钻孔、文件映射、加工说明和校验值。
- `source/Phased_Array_Ant_70x70_4L.brd`：修改后的 EAGLE 四层板。
- `source/Phased_Array_Ant_70x70_4L.kicad_pcb` / `.kicad_pro`：用于本次导出的 KiCad 四层板及规则。
- `preview/four_layer_gerber_preview.png`：由最终 Gerber 渲染的铜层预览。
- `verification/geometry_audit.json`：源文件、层数及几何比较。
- `verification/gerber_readback_audit.json`：光绘回读、实际连接、阻焊及钻孔检查。
- `verification/drc_4L.json`、`drc_6L.json`：完整 DRC 报告。
- `verification/build_cam.py`、`audit_gerbers.py`：本次修改/导入与独立回读脚本。

原始 EAGLE 文件 SHA-256：`6d23e7cc4e6d95c0f24957db893b5fbe539984da62e5cb00274a3205890b6aa1`。

工具参考：[KiCad 官方 CLI 文档](https://docs.kicad.org/10.0/en/cli/cli.html)、[KiCad EAGLE 格式说明](https://dev-docs.kicad.org/en/import-formats/eagle/index.html)。
